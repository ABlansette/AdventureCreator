@model IEnumerable<AdventurerCreator.Models.Adventurer.BadGuyListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BadGuyId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsBoss)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanetName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BadGuyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsBoss)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.BadGuyId }) |
                @Html.ActionLink("Details", "Details", new { id = item.BadGuyId }) |
                @Html.ActionLink("Delete", "Delete", new {id= item.BadGuyId})
            </td>
        </tr>
    }

</table>

<a href="../Home/Settings" class="btn btn-primary btn-lg">Back</a>

